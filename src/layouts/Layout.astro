<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Source+Serif+4:opsz,wght@8..60,400;8..60,600&family=Inter:wght@400;500&display=swap"
			rel="stylesheet"
		/>
		<title>Naylo's Tools</title>
	</head>
	<body>
		<div class="wrap">
			<nav>
				<a href="/">Naylo's Tools</a>
				<a href="/blog">Blog</a>
			</nav>
			<slot />
		</div>
	</body>
</html>

<style is:global>
	:root {
		--paper: #fafaf8;
		--ink: #1a1a1a;
		--ink-soft: #55534d;
		--accent: #433873;
		--rule: #e4e2db;
	}

	html, body { margin: 0; padding: 0; }

	body {
		background: var(--paper);
		color: var(--ink);
		font-family: 'Inter', system-ui, sans-serif;
		font-size: 17px;
		line-height: 1.6;
	}

	h1, h2, h3 {
		font-family: 'Source Serif 4', Georgia, serif;
		font-weight: 600;
		line-height: 1.25;
		color: var(--ink);
	}

	h1 { font-size: 2.1rem; margin: 0 0 1.25rem; }
	h2 { font-size: 1.5rem; margin: 2rem 0 0.75rem; }

	p { margin: 0 0 1.1rem; }

	a {
		color: var(--accent);
		text-decoration: none;
		border-bottom: 1px solid transparent;
	}
	a:hover, a:focus-visible { border-bottom-color: var(--accent); }

	ul { padding-left: 1.2rem; }
	li { margin-bottom: 0.4rem; }

	.wrap {
		max-width: 650px;
		margin: 0 auto;
		padding: 0 1.25rem 4rem;
	}

	nav {
		display: flex;
		justify-content: space-between;
		align-items: baseline;
		padding: 2.5rem 0;
		border-bottom: 1px solid var(--rule);
		margin-bottom: 2.5rem;
		font-size: 0.95rem;
	}
	nav a:first-child {
		font-family: 'Source Serif 4', Georgia, serif;
		font-weight: 600;
		font-size: 1.05rem;
		color: var(--ink);
		border-bottom: none;
	}
	nav a:last-child { color: var(--ink-soft); }
	nav a:last-child:hover { color: var(--accent); }
</style>